import SmsPage from '@/components/admin/SmsPage';

export const metadata = { title: '문자 관리 - 어드민', robots: { index: false, follow: false } };

export default function AdminSmsPage() {
  return <SmsPage />;
}
